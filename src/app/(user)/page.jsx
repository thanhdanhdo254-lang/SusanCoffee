export default async function Menu() {
  // Tạm thời comment đoạn fetch lại để test
  // const res = await fetch(...)
  
  return (
    <div className="container mt-5 pt-5 text-center">
      <h1>Trang chủ Susan Coffee</h1>
      <p>Nếu bạn thấy dòng này, có nghĩa là code đã lên Vercel thành công, lỗi nằm ở hàm FETCH!</p>
    </div>
  )
}